name: Rulebook Runtime Production Smoke

on:
  schedule:
    - cron: "17 */12 * * *"
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: rulebook-runtime-production-smoke
  cancel-in-progress: false

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "24"

      - name: Verify production Rulebook runtime boundary
        env:
          DECIDE_RULEBOOK_RUNTIME_SMOKE_API_KEY: ${{ secrets.DECIDE_RULEBOOK_RUNTIME_SMOKE_API_KEY }}
        run: npm run smoke:rulebook-runtime
